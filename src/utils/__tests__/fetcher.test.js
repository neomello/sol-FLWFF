import fetcher from "../fetcher";

describe("fetcher", () => {
  beforeEach(() => {
    fetch.mockClear();
  });

  describe("fetcher", () => {
      const url = `/api`;

      const response = await fetcher(url);

    });
  });
});
